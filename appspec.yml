version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ssm-user/ticat
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ticat
    group: ticat

hooks:
  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
